import React from "react";
import styled from "styled-components";

const Title = () => {
  return <Text></Text>;
};

export default Title;

const Text = styled.h2`
  font-size: 20px;
  font-family: serif;
`;
